{% extends "base.twig" %}

{% block content %}
  {% set image = TimberImage(section_home.section_bg_image) %}
  <section class="error-404 has-bg" {% if image %}style="background-image:url('{{image.src|resize(1680)}}');"{% endif %}>
    <div class="center">
  	  <div class="section-header container"><h1>{{ __("Page not found", 'sage') }}</h1></div>
      <div class="section-content container">
        <div class="alert alert-warning text-center">
          <p>{{ __("Sorry, we couldn't find what you're looking for.", 'sage') }}</p>
        </div>
      </div>
    </div>
  </section>
{% endblock %}